version: '3'
services:
  app:
    environment:
      - TZ=Asia/Tokyo
    build:
      context: .
    platform: linux/amd64
    volumes:
      - './config.json:/ai/config.json:ro'
      - './font.ttf:/ai/font.ttf:ro'
      - './data:/ai/data'
    restart: always
